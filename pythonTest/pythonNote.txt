__name__ هي متغير خاص موجود في كل ملف Python، وبيأخذ قيمة تختلف حسب طريقة تشغيل الملف.

if __name__ == "__main__":
    skills_app.run()
هذا شرط حتى يشغل السيرفر فقط إذا شغّلت الملف بشكل مباشر.

هذا يسمى decorator (مزخرف).
معناه: لما المستخدم يفتح الرابط الأساسي (يعني /)، يتم تشغيل الدالة اللي تحته.

skills_app = Flask(__name__)
هنا يتم إنشاء نسخة من تطبيق Flask وتخزينها في المتغير skills_app.
Flask(__name__): يُخبر Flask بأن هذا الملف هو نقطة بداية التطبيق


render_template هي دالة من مكتبة Flask، وظيفتها إنها:
ترجع ملف HTML من مجلد القوالب (templates) وتعرضه في المتصفح

{{ ... }}
هاي الأقواس معناها إنك تكتب كود Python داخل HTML باستخدام Jinja2 (نظام القوالب في Flask).

url_for() هي دالة في Flask تولّد رابط URL لأي ملف أو مسار في التطبيق.

conn = get_db_connection()
🔹 هذا السطر: يستدعي دالة get_db_connection() التي تكون معرفة مسبقًا، ومهمتها إنشاء اتصال بقاعدة البيانات (Database).
✅ النتيجة: إنشاء كائن اسمه conn يمثل الاتصال بقاعدة البيانات.


